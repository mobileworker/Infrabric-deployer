apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - gaie-manifests-configmap.yaml
  - install-gaie-job.yaml
  - envoyfilter.yaml

commonLabels:
  component: inferencepool-controller
