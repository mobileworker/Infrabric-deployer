kind: Kustomization
apiVersion: kustomize.config.k8s.io/v1beta1
resources:
- namespace.yaml
# ArgoCD cluster-admin permissions - required BEFORE root-app deployment
- gitops-cluster-admin.yaml
# Optional secrets - create from .example.yaml files if needed
#- 99-loki-secret.yaml
#- 99-ssh-key-secret.yaml
- root-app.yaml
