apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
- ./appproject.yaml
- ./gitopsservice.yaml
- ./argocd-cm-patch.yaml
# Note: argocd-health-monitor.yaml is deployed in 00-prerequisites.yaml
# to ensure it's running BEFORE root-app deploys (prevents chicken-and-egg issues)