apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - namespace.yaml
  - operatorgroup.yaml
  - subscription.yaml
  # SR-IOV operator is now used ONLY for RoCE devices
  # InfiniBand devices are handled by NVIDIA Network Operator (see 27-nvidia-network-operator)
